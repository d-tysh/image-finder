import styled from "@emotion/styled";

export const ImageGalleryList = styled.ul`
    display: flex;
    flex-wrap: wrap;
    gap: 8px;
    margin: 20px;
    padding: 0;
    list-style: none;
    justify-content: center;
`;